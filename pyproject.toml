[project]
name = "restaurant-recommendation-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Milad15A", email = "miladataei1993@yahoo.com" }
]
requires-python = ">=3.11"
dependencies = [
    "ollama>=0.6.2",
]

[project.scripts]
restaurant-recommendation-ai = "restaurant_recommendation_ai:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
